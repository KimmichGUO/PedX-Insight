# All dependencies pinned to their newest releases (July 2026), including the numpy 2.5.x
# and OpenCV 5.x majors.
#
# paddlex was intentionally REMOVED: it hard-caps numpy<2.4 and pins
# opencv-contrib-python==4.10.0.84, which made the newest numpy/OpenCV uninstallable
# (pip ResolutionImpossible). Consequence: modules/age_gender (--mode ag, age/gender/race)
# depends on paddlex and is DISABLED until reimplemented on another model, or until paddlex
# is installed in a separate environment. See README.md and AGENTS.md.
filterpy>=1.4.5
lap>=0.5.13
matplotlib>=3.11.0
numpy>=2.5.1
opencv_contrib_python>=5.0.0.93
opencv_python>=5.0.0.93
pandas>=3.0.3
Pillow>=12.3.0
scipy>=1.18.0
seaborn>=0.13.2
Shapely>=2.1.2
scikit-image>=0.26.0
supervision>=0.29.1
torch>=2.13.0
torchvision>=0.28.0
ultralytics>=8.4.92

# Optional companion tool: Monocular-OSM-Localization (geolocates a video on OpenStreetMap
# given the video + city name). It is now a real pip package -- distribution
# `monocular-osm-localization`, import name `monocular_osm` -- run as a subprocess by
# modules/localization/localize.py. It is kept OUT of this core file on purpose: its geo/CV
# stack (osmnx/open3d/geopandas/pycolmap/...) is heavy and most PedX runs never localize.
# Install it only when you need localization -- easiest into this same venv, so --mode localize
# needs no separate interpreter:
#   pip install -r requirements-localize.txt   # (+ ffmpeg on PATH)
# then: python main.py --mode localize --source_video_path VIDEO --city "City, Country"
#   [--osm_python PYTHON]   (auto-uses PedX's venv if `monocular_osm` is importable there,
#    else $OSM_LOCALIZATION_PYTHON)
