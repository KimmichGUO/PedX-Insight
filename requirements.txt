# All dependencies pinned to their newest releases (July 2026), including the numpy 2.5.x
# and OpenCV 5.x majors.
#
# paddlex was intentionally REMOVED: it hard-caps numpy<2.4 and pins
# opencv-contrib-python==4.10.0.84, which made the newest numpy/OpenCV uninstallable
# (pip ResolutionImpossible). Consequence: modules/age_gender (--mode ag, age/gender/race)
# depends on paddlex and is DISABLED until reimplemented on another model, or until paddlex
# is installed in a separate environment. See README.md and AGENTS.md.
filterpy>=1.4.5
lap>=0.5.13
matplotlib>=3.11.0
numpy>=2.5.1
opencv_contrib_python>=5.0.0.93
opencv_python>=5.0.0.93
pandas>=3.0.3
Pillow>=12.3.0
scipy>=1.18.0
seaborn>=0.13.2
Shapely>=2.1.2
scikit-image>=0.26.0
supervision>=0.29.1
torch>=2.13.0
torchvision>=0.28.0
ultralytics>=8.4.92

# Optional companion tool: Monocular-OSM-Localization (geolocates a video on OpenStreetMap
# given the video + city name). Vendored as a git submodule at
#   external/Monocular-OSM-Localization
# pinned to its 0.1.0 release. It publishes no installable package (source-only, no setup.py/
# pyproject.toml, not on PyPI), so it is NOT a pip dependency and is not listed here; it is run
# as a subprocess by modules/localization/localize.py. Its 0.1.0 pins (numpy>=2.5,
# opencv-python>=4.13, osmnx, ...) are compatible with the versions above, so its requirements
# CAN be installed into this same venv -- then --mode localize runs with PedX's own interpreter,
# no separate env needed. (Its stack is still heavy: osmnx/open3d/geopandas/pycolmap.)
#   git submodule update --init external/Monocular-OSM-Localization        # checks out 0.1.0
#   pip install -r external/Monocular-OSM-Localization/requirements.txt    # (+ ffmpeg on PATH)
# then: python main.py --mode localize --source_video_path VIDEO --city "City, Country"
#   [--osm_python PYTHON]   (auto-uses PedX's venv if the tool's deps are importable there,
#    else the submodule's .venv or $OSM_LOCALIZATION_PYTHON)
